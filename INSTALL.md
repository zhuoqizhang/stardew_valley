# MyFirstMod 安装指南

> 这是一份面向新玩家的安装说明。如果你在用 AI 助手（比如 Claude）帮你安装，
> 可以直接把这份文档发给它，让它按顺序引导你完成每一步。

## 这个 Mod 是什么

给《星露谷物语》皮埃尔的商店加了一套"期货交易"系统：玩家可以预先签约在未来某天以约定价格
交割啤酒/淡啤酒，价格会围绕游戏原生售价动态浮动。**支持真正的远程联机多人游戏**（共享资金池，
任意玩家可以交割任意人签的合约）。

## ⚠️ 多人游戏重要提示

**联机模式下，房间里的每一位玩家都必须安装这个 Mod，不只是房主。**
这个 Mod 采用"房主权威"架构——如果只有房主装了 Mod，其他玩家会看不到期货相关的商店选项、
无法签约或交割，可能导致游戏行为不一致甚至报错。请确保所有人都完成本文档的安装步骤后再一起联机。

## 前置条件

在安装这个 Mod 之前，你需要：

1. **拥有《星露谷物语》本体**（Steam / GOG / Xbox Game Pass 均可，建议版本 1.6 及以上）
2. **安装好 SMAPI**（星露谷的 Mod 加载器）——如果还没装，先完成下面第1步

## 安装步骤

### 第1步：安装 SMAPI（如果尚未安装）

1. 访问 SMAPI 官方下载页：https://smapi.io/
2. 根据你的操作系统（Windows / Mac / Linux）下载对应的安装程序
3. 运行安装程序，它会自动检测你的星露谷安装位置（如果检测失败，需要手动指定游戏安装目录）
4. 安装完成后，**通过 SMAPI 启动一次游戏**（桌面上会多出一个"StardewModdingAPI"或类似名字的启动图标，
   以后都要用这个图标启动游戏，而不是原来的游戏图标），确认能正常进入游戏主菜单，
   如果能正常进入说明 SMAPI 装好了，可以关闭游戏进行下一步

### 第2步：找到你的 Mods 文件夹

根据操作系统不同，Mods 文件夹的位置是：

- **Windows（Steam版）**：`C:\Program Files (x86)\Steam\steamapps\common\Stardew Valley\Mods`
- **Windows（GOG版）**：一般在你安装游戏的目录下，找到 `Mods` 文件夹
- **Mac**：`~/Library/Application Support/Steam/steamapps/common/Stardew Valley/Contents/MacOS/Mods`
- **Linux**：`~/.steam/steam/steamapps/common/Stardew Valley/Mods`

> 如果找不到，最简单的办法是打开 Steam，右键星露谷物语 → 管理 → 浏览本地文件，
> 进去之后应该能看到 `Mods` 这个文件夹（如果 SMAPI 已经装好，这个文件夹应该已经存在）。

### 第3步：下载并放置 Mod 文件

1. 从这个仓库下载最新版本（点击仓库页面的 "Code" → "Download ZIP"，或者直接下载 Release 里打包好的版本）
2. 解压下载下来的压缩包
3. 你应该会得到一个名为 `MyFirstMod` 的文件夹，里面包含 `manifest.json` 和一个 `.dll` 文件
4. 把整个 `MyFirstMod` 文件夹**完整地**复制到第2步找到的 `Mods` 文件夹里

**正确的目录结构应该长这样：**

```
Mods/
└── MyFirstMod/
    ├── manifest.json
    └── MyFirstMod.dll
```

⚠️ 常见错误：不要把 `MyFirstMod` 文件夹里的文件直接散放进 `Mods` 文件夹，
必须保留 `MyFirstMod` 这一层文件夹结构。

### 第4步：启动游戏验证

1. 通过 SMAPI 的启动图标启动游戏（不是原版游戏图标）
2. SMAPI 会弹出一个控制台窗口（黑底文字），游戏加载时这个窗口会滚动很多日志
3. 在日志里找到类似这样的一行，确认 Mod 被正确加载：

```
[MyFirstMod] Registered PierreShopPatch...
```

   或者任何以 `[MyFirstMod]` 开头的日志行，只要看到这类日志说明 Mod 已经被 SMAPI 正确识别并加载
4. 如果日志里出现红色的错误信息、或者提示 "Failed to load mod"，说明安装有问题，
   常见原因是：星露谷版本太旧、SMAPI版本和游戏版本不匹配、或者第3步的文件夹结构放错了

### 第5步：进入游戏测试

1. 进入你的存档，走到皮埃尔的商店门口
2. 点击商店，正常情况下会弹出一个"购买商品 / 期货交易 / 算了"的三选一对话
3. 选择"期货交易"，应该能看到"买卖期货"和"当日交割"两个标签页

如果看到了这个界面，说明安装完全成功！

## 遇到问题怎么办

- **游戏没有 SMAPI 控制台窗口弹出**：说明你可能是用原版游戏图标启动的，请找到 SMAPI 的启动图标重新启动
- **点击皮埃尔商店没有弹出三选一对话，直接进了原版商店**：检查 Mods 文件夹里的文件结构是否正确（第3步）
- **联机时其他人看不到期货功能**：确认对方也完整走完了本文档的所有步骤，特别是第3步的文件放置
